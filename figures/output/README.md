# Figure artifact status

These PNGs are regenerated from the legacy exploratory reports under
`outputs/reports/`. They are useful for checking the plotting code and the
historical snapshot, but they are not current submission figures. Regenerate
figures only after the current immutable run artifacts pass the null,
provenance, causal-control, and scorer gates.
